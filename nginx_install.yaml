---
- name: Install nginx and MySQL
  hosts: web
  become: yes
  tasks:
    - name: Install nginx and MySQL
      yum:
        name:
          - nginx
          - mysql
        state: present

    - name: Start Nginx service
      service:
        name: nginx
        state: started
      become: true
