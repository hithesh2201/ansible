- name: Install nginx and configure it
  hosts: shell
  become: yes
  tasks:
    - name: Install nginx
      yum:
        name: nginx
        yum: nginx,mysql
        state: present
    - name: Start Nginx service
      service:
        name: nginx
        state: started

      
