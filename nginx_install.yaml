---
- name: Install nginx and MySQL
  hosts: shell
  become: yes
  tasks:
    - name: Install nginx and MySQL
      yum:
        name:
          - nginx
          - mysql
        state: present

    - name: Start Nginx service
      service:
        name: nginx
        state: started
      become: true
